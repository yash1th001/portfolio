name: Deploy Portfolio to GitHub Pages

on:
  push:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 11.17.0

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: 'pnpm'

      - name: Install Dependencies
        run: pnpm install --frozen-lockfile

      - name: Build
        working-directory: artifacts/portfolio
        env:
          NODE_ENV: production
          PORT: '5000'
        run: pnpm run build

      - name: Verify build output
        run: |
          echo "=== dist/ contents ==="
          ls -la artifacts/portfolio/dist/
          echo "=== assets/ contents ==="
          ls -la artifacts/portfolio/dist/assets/
          echo "=== index.html script tags ==="
          grep -E "script|link rel=\"stylesheet\"" artifacts/portfolio/dist/index.html
          echo "=== CNAME ==="
          cat artifacts/portfolio/dist/CNAME

      - name: Setup Pages
        uses: actions/configure-pages@v5

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: artifacts/portfolio/dist

  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
